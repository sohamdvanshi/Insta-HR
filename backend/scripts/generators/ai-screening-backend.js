// Generator: creates aiScreening controller + routes
// Run from backend/: node scripts/generators/ai-screening-backend.js
const fs = require('fs');
const path = require('path');
const srcBase = path.join(__dirname, '../../src');

console.log('Building AI Screening backend...');

// This is the same as STEP1-backend.js but kept as a separate reference.
// Run STEP1-backend.js for a full setup.
require('./STEP1-backend.js');
